<h3> My bookings as a renter </h3>

 <% @bookings.each do |booking| %>
    <% if booking.status == "Confirmed" && booking.user == current_user %>
      <div class="pet-booking">
        <div class="pet-photo">
          <%= cl_image_tag booking.pet.photo %>
        </div>
        <div>
          <h3> <%= booking.pet.name %> </h3>
          <p> <%= booking.pet.price%>€ / day </p>
          <p> Owner: <%= booking.pet.user.first_name %> <%= booking.pet.user.last_name %></p>
        </div>
        <ul class="list-inline pet-actions">
          <li><%= link_to "Cancel", edit_booking_path(booking) %><li>
        </ul>
      </div>
    <% end %>
  <% end %>

<h3> My bookings as an owner </h3>

 <% @bookings.each do |booking| %>
    <% if booking.status == "Confirmed" && booking.pet.user == current_user %>
      <div class="pet-booking">
        <div class="pet-photo">
          <%= cl_image_tag booking.pet.photo %>
        </div>
        <div>
          <h3> <%= booking.pet.name %> </h3>
          <p> <%= booking.pet.price%>€ / day </p>
          <p> Renter: <%= booking.user.first_name %> <%= booking.user.last_name %></p>
        </div>
        <ul class="list-inline pet-actions">
          <li><%= link_to "Cancel", edit_booking_path(booking) %><li>
        </ul>
      </div>
    <% end %>
  <% end %>


  <h1>Cancelled Bookings as a renter</h1>
  <% @bookings.each do |booking| %>
    <% if booking.status == "Cancelled" && booking.user == current_user %>
      <div class="pet-booking">
        <div class="pet-photo">
          <%= cl_image_tag booking.pet.photo %>
        </div>
        <div>
          <h3> <%= booking.pet.name %> </h3>
          <p> <%= booking.pet.price%>€ / day </p>
          <p> Owner: <%= booking.pet.user.first_name %> <%= booking.pet.user.last_name %></p>
        </div>
        <ul class="list-inline pet-actions">
          <li><%= link_to "Remove from history", booking_path(booking), method: "delete" %><li>
        </ul>
      </div>
    <% end %>
  <% end %>

   <h1>Cancelled Bookings as a owner</h1>
  <% @bookings.each do |booking| %>
    <% if booking.status == "Cancelled" && booking.pet.user == current_user %>
      <div class="pet-booking">
        <div class="pet-photo">
          <%= cl_image_tag booking.pet.photo %>
        </div>
        <div>
          <h3> <%= booking.pet.name %> </h3>
          <p> <%= booking.pet.price%>€ / day </p>
          <p> Renter: <%= booking.user.first_name %> <%= booking.user.last_name %></p>
          <p> Booking status: <%= booking.status %></p>
        </div>
        <ul class="list-inline pet-actions">
          <li><%= link_to "Remove from history", booking_path(booking), method: "delete" %><li>
        </ul>
      </div>
    <% end %>
  <% end %>
